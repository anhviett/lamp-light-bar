import HeroCarouselClient from "@/components/landing/hero-carousel-client";
import { HeroSlideContent } from "@/components/landing/hero-slide-content";

export function HeroCarousel() {
  return (
    <HeroCarouselClient>
      <HeroSlideContent />
    </HeroCarouselClient>
  );
}
